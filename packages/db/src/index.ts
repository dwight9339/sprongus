/**
 * Database package stub.
 * Wire Drizzle client and schema here later.
 */

export type DatabaseClient = unknown;

export function createClient(): DatabaseClient {
  // TODO: initialize a Drizzle client (e.g., with Postgres/SQLite)
  return {} as DatabaseClient;
}

